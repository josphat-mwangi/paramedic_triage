.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
*.iml
.idea/
.vscode/
ios/Pods/
android/.gradle/

# Mobile-only assessment (Android/iOS) — desktop/web scaffolding from a
# `flutter create .` re-run without --platforms is not part of this project.
/linux/
/macos/
/web/
/windows/

# Docs: keep the repo clean — only README.md is tracked.
# AGENTS.md and .claude/agents/*.md stay local working docs.
*.md
!README.md
